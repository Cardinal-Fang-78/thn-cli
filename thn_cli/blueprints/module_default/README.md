# THN Default Module Blueprint

This folder describes the default structure of a THN Module.

Files the CLI uses:
- `blueprint.json` — metadata
- `directories.txt` — folders created when making a module
- `README.md` — informational only
