# THN Default Project Blueprint

This folder describes the default structure of a THN Project.

Files the CLI uses:
- `blueprint.json` — metadata
- `directories.txt` — folders created when making a project
- `README.md` — informational only
