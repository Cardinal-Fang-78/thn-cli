{
    "project": "{{ project_name }}",
    "owner": "{{ owner }}",
    "generated": "{{ created }}",
    "paths": {
        "modules_dir": "modules",
        "assets_dir": "assets",
        "config_dir": "config",
        "docs_dir": "docs",
        "logs_dir": "logs",
        "temp_dir": "temp"
    }
}
